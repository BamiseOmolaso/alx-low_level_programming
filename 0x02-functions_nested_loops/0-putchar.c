#include "main.h"

/**
 * main -  prints _putchar, followed by a new line
 * Return: Always 0 (Success)
 */

int main(void)
{
	_putchar('_');
	_putchar('p' + 'u' + 't' + 'c' + 'h' + 'a' + 'r');
	_putchar('\n');

	return (0);
}

